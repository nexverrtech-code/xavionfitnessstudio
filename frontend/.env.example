# Public configuration only — never put secrets in the frontend.
# Leave empty when the Worker serves /api on the app's own domain (recommended),
# or set the Worker's origin, e.g. https://api.gymname.com
VITE_API_URL=
