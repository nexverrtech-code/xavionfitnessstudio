"""Scheduled (cron) jobs.

Named ``jobs`` rather than ``workers``: ``workers`` is the Cloudflare Python Workers SDK module
(``from workers import WorkerEntrypoint``) and a same-named package would shadow it.
"""
